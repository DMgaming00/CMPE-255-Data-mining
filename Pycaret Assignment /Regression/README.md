# 📈 Regression

This notebook uses **PyCaret’s regression module** to predict continuous numeric values.

## 📘 Overview
- Dataset: *House Prices Dataset*
- Target: `SalePrice`
- Compared multiple regression models automatically
- Selected the best model based on R² and RMSE

## ⚙️ Steps
1. Loaded and explored dataset
2. Ran `setup()` for regression
3. Used `compare_models()` to find the best algorithm
4. Finalized model and generated predictions

## 📊 Outcome
- Predicted house prices accurately
- Created `submission.csv` and `best_regression_model.pkl`
