# 🌸 Multiclass Classification

This notebook demonstrates **multiclass classification** using the **Iris dataset** with PyCaret.

## 📘 Overview
- Target: `Species` (Setosa, Versicolor, Virginica)
- Used PyCaret’s `setup()` and `compare_models()`
- Evaluated the best model using confusion matrix and accuracy score

## ⚙️ Steps
1. Loaded and cleaned the Iris dataset
2. Initialized PyCaret for multiclass classification
3. Compared and trained multiple models
4. Evaluated performance and saved final model

## 📊 Outcome
- Successfully classified flower species
- Saved `best_multiclass_model.pkl` and prediction outputs
